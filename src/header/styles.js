const {colors, spacing} = require('../variables/all');

module.exports = {

    base: {
        background: colors.white,
        border: `1px solid ${colors.gray['100']}`,
        height: '100%'
    }

};
