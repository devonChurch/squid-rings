const {colors, spacing} = require('../variables/all');

module.exports = {

    base: {
        background: colors.white,
        boxShadow: `0px 0px 10px 0px ${colors.black}`,
        padding: spacing.large
    }

};
